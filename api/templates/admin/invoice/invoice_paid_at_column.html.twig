{% extends get_admin_template('base_list_field', admin.code) %}

{% block field %}
    {% if object.paymentDate is not null %}
        <i class="fa fa-calendar"></i>
        {{ object.paymentDate | localizeddate('medium', 'none') }}
    {% endif %}

{% endblock %}
