{% extends get_admin_template('base_list_field', admin.code) %}

{% block field %}
    <span class="label label-success label-square">{{ object.numOfStudents }}</span> /
    <span class="label label-primary label-square">{{ object.numOfPlaces }}</span>
{% endblock %}
